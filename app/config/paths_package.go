//go:build package

package config

// DefaultConfigPath is the default location of the configuration file for
// DENNIS, which may be configured at runtime using the `--config` flag.
const DefaultConfigPath = `/etc/dennis/config.yml`
